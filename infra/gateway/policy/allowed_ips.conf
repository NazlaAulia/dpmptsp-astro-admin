# admin_access = allowed_ips  (default)
#
# geo evaluates $remote_addr AFTER realip.conf has rewritten it, so this
# compares against the real client address rather than the docker bridge.
geo $admin_ip_ok {
    default 0;
    include /etc/nginx/policy/allowed_ips.list;
}
