# admin_access = public
#
# No network restriction; application auth only.
#
# Your own stack/apps.yml records the precedent for this: Adminer was disabled
# because exposing an admin login naked was judged wrong, with the note that if
# it is genuinely needed it belongs behind Cloudflare Access so authentication
# happens at the edge. The same applies here.
