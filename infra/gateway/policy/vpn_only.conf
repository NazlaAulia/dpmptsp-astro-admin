# admin_access = vpn_only
#
# NOT USABLE YET: no VPN runs on this machine. Fill in the CIDR before
# selecting this policy — the entrypoint refuses to start otherwise, which is
# better than silently serving 403 to everyone and having it debugged as a bug.
geo $admin_ip_ok {
    default 0;
    # 100.64.0.0/10 1;   # Tailscale
    # 10.8.0.0/24 1;     # WireGuard
}
