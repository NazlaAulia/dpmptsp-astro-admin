---
// Entry point for /admin. Authentication is handled by src/middleware.ts, so
// this only has to decide where an authenticated visitor lands.
export const prerender = false;

return Astro.redirect("/admin/dashboard");
---
