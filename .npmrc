# Astro and Vite resolve correctly under pnpm's isolated node_modules.
strict-peer-dependencies=false
auto-install-peers=true
# Makes "workspace:*" deps resolve to the local package instead of the registry.
link-workspace-packages=deep
prefer-workspace-packages=true
